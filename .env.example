# Backend API adresi (sonunda /api olacak, sonda eğik çizgi yok).
#
# Boş bırakılırsa üretim backend'i kullanılır:
#   https://scalable-leaderboard-engine.onrender.com/api
#
# Vercel'de ayarlamak için: Project Settings -> Environment Variables
# Yalnızca VITE_ önekli değişkenler istemci paketine dahil edilir.
# Bu değer derleme anında paketin içine gömülür; gizli bilgi koyma.
VITE_API_BASE=https://scalable-leaderboard-engine.onrender.com/api
